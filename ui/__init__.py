"""Web UI package for graphwar-sim.

A pure additive layer: a FastAPI server (``ui.server``) plus a static,
no-build-step frontend (``ui/static/``) that wraps the already-tested
``graphwar_sim`` core. See ``docs/UI_GROUND_TRUTH.md`` for the visual spec.
"""
