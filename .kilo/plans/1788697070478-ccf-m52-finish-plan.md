# M5.2 CCF — Finish Plan (post-handoff continuation)

Source of truth: `5.2.md` (M5.2 spec v2) + handoff `/var/folders/.../graphwar_m52_handoff.md`. Branch `main`, HEAD `fa619e5`.

## Current state (verified)

- **Committed** `fa619e5` "M5.2 §8: layered-DAG branch selection (_branch_paths)": `graphwar_sim/ccf.py` §8 DP + `docs/OPEN_QUESTIONS.md` entries (i) (pixel-cell fidelity) and (j) (K-paths deviation). Verified before commit: mypy --strict, ruff, 40 tests green, 24-seed smoke 8 CERTIFIED / 15 UNCERTIFIED / 1 BASIS_INFEASIBLE.
- **Dirty, verified green (uncommitted)**:
  - `graphwar_sim/solver.py` + `tests/test_solver.py` — CCF rung integration: `RUNG_CCF`, lazy `_ordered_candidates` generator (order: per_target ×3 → parabola → line → **ccf** → fixed_grid → arc), `_Frame.circles`/`teammates`, `cert` on `_Candidate`/`SolverResult` (TYPE_CHECKING import), notes summary. mypy/ruff clean; 19 pass.
  - `tests/test_ccf.py` — 12 pass (~12 s): curvature bound via real parser, J_max, degenerates, MILP timeout sentinel, no-sci-notation, grep regressions.
  - Golden harness — `tools/golden/generate_ccf_golden.py` + `tests/golden/data/ccf_golden.json` (8 scenarios, seeds 1–9, 4 certified / 10 uncertified) + `tests/golden/test_ccf_golden.py` (30 pass, in-sim + Java replay).
- **Dirty, FAILING (correctly)**: `tests/test_ccf_property.py` — 200 maps, ~7 min: 10 CERTIFIED-but-collided cases, ALL `mode="tight"` (seeds 6, 7, 14, 24, 75, 140×2, 144, 151, 195).
- **Dirty, applied but UNVERIFIED**: `graphwar_sim/ccf.py` (+19/−5) — the tight-mode gate frame fix, already patched by the interrupted agent:
  - `_finalize_solution` now calls `_posthoc_gate(lambda u: g(u) - my, ...)` — the gate checks the FIRED RELATIVE curve `f_ccf + delta` against relative bounds `Lrel/Hrel` (was comparing the ABSOLUTE curve: off by `my`).
  - Docstrings of `_fired_curve` + `_posthoc_gate` updated with the frame contract.

## Root cause (established)

`_fired_curve` returns `g(u) = f_ccf(u) + my + delta` (absolute). The old gate compared it against `Lrel = Lsamp − my` — a window shifted by `2·my`. The launch-nudge `delta` (constant offset, O(r_eff²·M)) went unmeasured exactly in tight corridors, so tight-mode certificates were unsound. Allowance mode is unaffected (charges `(3/2)·r_eff²/σ²·s` analytically).

## Tasks (in order)

1. **Verify the applied gate fix** (ccf.py is already patched — do NOT re-edit unless step fails):
   - `mypy --strict graphwar_sim/ccf.py`; `ruff check graphwar_sim/ccf.py && ruff format --check graphwar_sim/ccf.py`.
   - `python3 -m pytest tests/test_ccf_property.py -q` (~7 min). Expect **zero** CERTIFIED-but-collided. If any remain: keep diagnosing (delta is constant, so gate-at-samples + chord argument covers between-samples; follow evidence, fix mechanism, never tune constants).
   - 24-seed smoke (`Game.create(seed, num_soldiers=2)`, `solve_for_game`): record new distribution (CERTIFIED count drops — false tight certs become rejections; that is honest) and count `solve_cap_reached` traces (`_MAX_SOLVES_PER_TARGET=24` vs 5σ×4 branches×2 modes=40 — report only, retune only if the battery shows ladder starvation, as a `# TUNABLE` change).
   - Full relevant suite: `tests/test_solver.py` (~4–5 min, battery tests traverse real CCF), `tests/test_corridor.py`, `tests/test_emission.py`, `tests/test_ccf.py`, `tests/golden/test_ccf_golden.py`, `tests/golden/test_golden.py`. Golden stored expressions were in-sim-verified pre-fix → should stay green; if not, regenerate via `tools/golden/generate_ccf_golden.py` (mechanical, report it).
2. **Commit slices** (repo convention, short subjects, "M5.2 ..." prefix; never mix unrelated files; NO push yet):
   - ccf.py gate fix + docstring: e.g. "M5.2: tight-mode gate checks the relative fired curve" (body: mechanism + property-test evidence).
   - Solver integration: `graphwar_sim/solver.py`, `tests/test_solver.py`.
   - Tests + golden: `tests/test_ccf.py`, `tests/test_ccf_property.py`, `tests/golden/test_ccf_golden.py`, `tests/golden/data/ccf_golden.json`, `tools/golden/generate_ccf_golden.py`.
3. **Battery + eval/REPORT.md M5.2 section** (§12): per-outcome distributions, rung histogram diffed vs M4 board (SOLVER_FAILED=297, per_target=7, parabola=2, arc=12, line=3, fixed_grid=1), emitted-length distribution, σ/branch usage, MILP firing rate, solve wall-clock distributions (not means). Long run → `nohup … &` + `tail`; `tools/time_tests.py` as watchdog.
4. **§13 acceptance/kill decision** from the battery, reported plainly: ship iff CERTIFIED rate on solvable maps > fixed_grid, median emitted length shorter, zero CERTIFIED-but-collided in the property test, `dud` bucket materially shrunk. Kill if CCF < 10% of rungs or EMIT_OVERFLOW dominates. Do not loosen margins to recover numbers.
5. **Fixed-grid death row**: if CCF beats fixed_grid on the same seeds → delete the fixed_grid rung (end state 4 rungs, arc last), update `tests/test_solver.py` accordingly; otherwise record why it survives.
6. **Final**: mypy --strict + ruff across `graphwar_sim/`, full test suite, commit remaining slices (REPORT.md, death-row change), then **push to origin**.

## Execution notes

- Subagent environments have NO Edit/Write tools — patch files via bash inline Python exact-string replacement (`python3 - <<'PY'` … `assert src.count(old) == 1`), verify each hunk with `git diff`, confirm import after each patch.
- Only `graphwar_sim/ccf.py` may change logic in task 1; tests are never weakened; constants are never tuned to hide symptoms.
- If the property test still fails after the gate fix, the next suspects are (in order): `_fired_curve`'s fidelity to `process_function_range`'s muzzle setup (angle fixed point + offset), the chord argument's assumption that delta is constant across the whole span, and `exempt` masking near the goal column.
- OPEN_QUESTIONS (i)/(j) are already committed — no further docs work unless the battery surfaces a new recorded divergence.
